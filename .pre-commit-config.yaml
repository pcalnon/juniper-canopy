#####################################################################################################################################################################################################
# Project:       Juniper
# Sub-Project:   JuniperCanopy
# Application:   juniper_canopy
# File Name:     .pre-commit-config.yaml
# Author:        Paul Calnon
# Version:       1.2.0
#
# Date Created:  2025-11-03
# Last Modified: 2026-01-31
#
# License:       MIT License
# Copyright:     Copyright (c) 2024-2026 Paul Calnon
#
# Description:
#    Pre-commit hooks configuration for Juniper Canopy
#    Runs code quality checks locally before commit and in CI
#
# Setup:
#    pip install pre-commit
#    pre-commit install
#    pre-commit run --all-files
#
# References:
#    - https://pre-commit.com/
#####################################################################################################################################################################################################

# Minimum pre-commit version required
minimum_pre_commit_version: "3.0.0"

# Default settings for all hooks
default_language_version:
  python: python3.14

# Exclude patterns (files/directories to skip)
exclude: |
  (?x)^(
      \.git/|
      \.venv/|
      venv/|
      __pycache__/|
      \.pytest_cache/|
      \.mypy_cache/|
      data/|
      prompts/|
      reports/|
      resources/|
      logs/|
      images/|
      .*\.egg-info/|
      .*\.ipynb_checkpoints/.*|
      .*/backups/.*|
      .*-ORIG\.py$|
      .*\.bak$|
      .*\.orig$|
      .*~$
  )$

repos:
  # ═══════════════════════════════════════════════════════════════════════════
  # General file checks
  # ═══════════════════════════════════════════════════════════════════════════
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: check-yaml
        name: Check YAML syntax
      - id: check-toml
        name: Check TOML syntax
      - id: check-json
        name: Check JSON syntax
      - id: end-of-file-fixer
        name: Fix end of files
      - id: trailing-whitespace
        name: Trim trailing whitespace
        args: [--markdown-linebreak-ext=md]
      - id: check-merge-conflict
        name: Check for merge conflicts
      - id: check-added-large-files
        name: Check for large files
        args: [--maxkb=1000]
      - id: check-case-conflict
        name: Check case conflicts
      - id: check-ast
        name: Check Python AST
      - id: debug-statements
        name: Check for debug statements
      - id: detect-private-key
        name: Detect private keys

  # ═══════════════════════════════════════════════════════════════════════════
  # Python Code Formatting - Black
  # ═══════════════════════════════════════════════════════════════════════════
  - repo: https://github.com/psf/black
    rev: 25.1.0
    hooks:
      - id: black
        name: Format with Black
        args:
          - --line-length=512
        files: ^src/

  # ═══════════════════════════════════════════════════════════════════════════
  # Python Import Sorting - isort
  # ═══════════════════════════════════════════════════════════════════════════
  - repo: https://github.com/pycqa/isort
    rev: 5.13.2
    hooks:
      - id: isort
        name: Sort imports with isort
        args:
          - --profile=black
          - --line-length=512
        files: ^src/

  # ═══════════════════════════════════════════════════════════════════════════
  # Python Linting - Flake8 (source code)
  # Re-enabled B905 (zip strict=), F401 (unused imports), B008 (func in defaults)
  # ═══════════════════════════════════════════════════════════════════════════
  - repo: https://github.com/pycqa/flake8
    rev: 7.1.1
    hooks:
      - id: flake8
        name: Lint with Flake8
        args:
          - --max-line-length=512
          - --extend-ignore=E203,E265,E266,E501,W503,E722,E402,E226,C409,C901,B904,B907
          - --max-complexity=15
          - --select=B,C,E,F,W,T4,B9
        additional_dependencies:
          - flake8-bugbear>=24.0.0
          - flake8-comprehensions>=3.14.0
          - flake8-simplify>=0.21.0
        files: ^src/
        exclude: ^src/tests/

  # ═══════════════════════════════════════════════════════════════════════════
  # Python Linting - Flake8 (tests - relaxed)
  # ═══════════════════════════════════════════════════════════════════════════
  - repo: https://github.com/pycqa/flake8
    rev: 7.1.1
    hooks:
      - id: flake8
        name: Lint tests with Flake8 (relaxed)
        # Relaxed for tests: allow assert (S101), random (S311), bare except (E722)
        # F401 (unused imports) often needed for fixtures
        args:
          - --max-line-length=512
          - --extend-ignore=E203,E265,E266,E501,W503,E722,E402,E226,C409,C901,B008,B904,B905,B907,F401,S101,S311
          - --max-complexity=20
          - --select=B,C,E,F,W,T4,B9
        additional_dependencies:
          - flake8-bugbear>=24.0.0
          - flake8-comprehensions>=3.14.0
          - flake8-simplify>=0.21.0
        files: ^src/tests/

  # ═══════════════════════════════════════════════════════════════════════════
  # Python Type Checking - MyPy
  # ═══════════════════════════════════════════════════════════════════════════
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.13.0
    hooks:
      - id: mypy
        name: Type check with MyPy
        args:
          # - --install-types
          # - --non-interactive
          - --ignore-missing-imports
          - --no-strict-optional
          - --python-version=3.14
          # - --python-version=3.12
          # Re-enabled (Epic 4.3): call-arg, override, no-redef, index,
          # func-returns-value, has-type, str-bytes-safe, call-overload, return
          # Still disabled due to existing errors requiring gradual fixes:
          - --disable-error-code=attr-defined
          - --disable-error-code=return-value
          - --disable-error-code=arg-type
          - --disable-error-code=assignment
          - --disable-error-code=var-annotated
          - --disable-error-code=misc
          - --disable-error-code=dict-item
        files: ^src/
        exclude: ^src/tests/
        additional_dependencies:
          - types-PyYAML
          - types-redis
          - types-requests

  # ═══════════════════════════════════════════════════════════════════════════
  # Security Scanning - Bandit
  # ═══════════════════════════════════════════════════════════════════════════
  - repo: https://github.com/PyCQA/bandit
    rev: 1.7.9
    hooks:
      - id: bandit
        name: Security scan with Bandit
        args:
          - -q
          - --skip=B101,B311
        files: ^src/.*\.py$
        exclude: ^src/tests/

  # ═══════════════════════════════════════════════════════════════════════════
  # Markdown Linting
  # ═══════════════════════════════════════════════════════════════════════════
  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.42.0
    hooks:
      - id: markdownlint
        name: Lint Markdown files
        args:
          - --config=.markdownlint.yaml
          - --fix
        files: \.(md|markdown)$
        # Note: docs/ now included for linting (Epic 4.2.1)
        # Excluding notes/ (development notes) and CHANGELOG.md (auto-generated format)
        exclude: ^(CHANGELOG\.md|notes/|docs/history/)

  # ═══════════════════════════════════════════════════════════════════════════
  # Shell Script Linting - shellcheck
  # ═══════════════════════════════════════════════════════════════════════════
  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: v0.10.0.1
    hooks:
      - id: shellcheck
        name: Lint shell scripts
        args:
          - --severity=error
        files: \.(sh|bash)$

  # ═══════════════════════════════════════════════════════════════════════════
  # YAML Linting - yamllint
  # ═══════════════════════════════════════════════════════════════════════════
  - repo: https://github.com/adrienverge/yamllint
    rev: v1.35.1
    hooks:
      - id: yamllint
        name: Lint YAML files
        args: [--format, parsable, -d, relaxed]
        types: [yaml]

# CI configuration (for pre-commit.ci service, if used)
ci:
  autofix_prs: false
  autoupdate_schedule: monthly
